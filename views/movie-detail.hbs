<a href="/movies">Go Back</a>
<div>
    <img src={{movie.image}}>
    <h2>{{movie.title}}</h2>
    <h3>{{movie.director}}</h3>
    {{#each movie.stars}}
    <ul>
        <li>{{this}}</li>
    </ul>
    {{/each}}
    <p>{{movie.description}}</p>
    <p>{{movie.showtimes}}</p>
</div>